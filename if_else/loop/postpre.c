#include<stdio.h>
int main(){

//    int x = 5 ;
//    printf("%d\n", x);
//    printf("%d\n",x++); // x ++ means, use x, then increament
   // ++x means, first increament then use

//    x++;  // x = x + 1;
//    printf("%d\n",x);


    int x =4, y=0,z;
    while (x>=0) {
      if(x == y)
      break;
    else 
    printf("\n %d %d ",x,y);
      x--;
      y++;
    }  
    
    return 0;
}